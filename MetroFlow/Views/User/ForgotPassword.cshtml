@{
    Layout = "_Layout";
    ViewData["Title"] = "Forgot Password - MetroFlow";
    ViewData["HideLayoutParts"] = true; // hide navbar & footer
}
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<style>
    /* Enhanced floating animations matching OTP page style */
    @@keyframes float1 {
        0%, 100%

    {
        transform: translateY(0px) translateX(0px) scale(1);
        opacity: 0.3;
    }

    25% {
        transform: translateY(-20px) translateX(10px) scale(1.1);
        opacity: 0.6;
    }

    50% {
        transform: translateY(-10px) translateX(-15px) scale(0.9);
        opacity: 0.4;
    }

    75% {
        transform: translateY(-30px) translateX(5px) scale(1.05);
        opacity: 0.7;
    }

    }

    @@keyframes float2 {
        0%, 100%

    {
        transform: translateY(0px) translateX(0px) scale(0.8);
        opacity: 0.2;
    }

    33% {
        transform: translateY(-25px) translateX(-20px) scale(1.2);
        opacity: 0.5;
    }

    66% {
        transform: translateY(-5px) translateX(25px) scale(0.7);
        opacity: 0.3;
    }

    }

    @@keyframes float3 {
        0%, 100%

    {
        transform: translateY(0px) translateX(0px) scale(1.1);
        opacity: 0.4;
    }

    40% {
        transform: translateY(-15px) translateX(-10px) scale(0.9);
        opacity: 0.6;
    }

    80% {
        transform: translateY(-35px) translateX(15px) scale(1.3);
        opacity: 0.2;
    }

    }

    @@keyframes float4 {
        0%, 100%

    {
        transform: translateY(0px) translateX(0px) scale(0.9);
        opacity: 0.3;
    }

    30% {
        transform: translateY(-20px) translateX(20px) scale(1.1);
        opacity: 0.5;
    }

    70% {
        transform: translateY(-10px) translateX(-25px) scale(0.8);
        opacity: 0.4;
    }

    }

    @@keyframes float5 {
        0%, 100%

    {
        transform: translateY(0px) translateX(0px) scale(1.2);
        opacity: 0.25;
    }

    20% {
        transform: translateY(-18px) translateX(12px) scale(0.8);
        opacity: 0.4;
    }

    60% {
        transform: translateY(-8px) translateX(-18px) scale(1.4);
        opacity: 0.3;
    }

    90% {
        transform: translateY(-28px) translateX(8px) scale(0.9);
        opacity: 0.5;
    }

    }

    @@keyframes float6 {
        0%, 100%

    {
        transform: translateY(0px) translateX(0px) scale(0.7);
        opacity: 0.35;
    }

    45% {
        transform: translateY(-22px) translateX(-12px) scale(1.1);
        opacity: 0.2;
    }

    75% {
        transform: translateY(-12px) translateX(22px) scale(0.9);
        opacity: 0.45;
    }

    }

    /* Floating circle classes */
    .floating-circle-1 {
        animation: float1 8s ease-in-out infinite;
    }

    .floating-circle-2 {
        animation: float2 10s ease-in-out infinite 2s;
    }

    .floating-circle-3 {
        animation: float3 12s ease-in-out infinite 4s;
    }

    .floating-circle-4 {
        animation: float4 9s ease-in-out infinite 1s;
    }

    .floating-circle-5 {
        animation: float5 11s ease-in-out infinite 3s;
    }

    .floating-circle-6 {
        animation: float6 7s ease-in-out infinite 5s;
    }

    .floating-circle-7 {
        animation: float1 13s ease-in-out infinite 6s;
    }

    .floating-circle-8 {
        animation: float2 9s ease-in-out infinite 1.5s;
    }

    .floating-circle-9 {
        animation: float3 10s ease-in-out infinite 2.5s;
    }

    .floating-circle-10 {
        animation: float4 14s ease-in-out infinite 4.5s;
    }

    @@keyframes pulse-green {
        0%, 100%

    {
        box-shadow: 0 0 0 0 rgba(134, 239, 172, 0.7);
    }

    70% {
        box-shadow: 0 0 0 10px rgba(134, 239, 172, 0);
    }

    }

    @@keyframes slide-in {
        from

    {
        opacity: 0;
        transform: translateY(30px);
    }

    to {
        opacity: 1;
        transform: translateY(0);
    }

    }

    .pulse-animation {
        animation: pulse-green 2s infinite;
    }

    .slide-in-animation {
        animation: slide-in 0.6s ease-out forwards;
    }

    .gradient-bg {
        background: linear-gradient(135deg, #f0fdf4 0%, #dcfce7 50%, #bbf7d0 100%);
    }

    /* Email input focus styling */
    .email-input:focus {
        border-color: rgb(22, 163, 74);
        box-shadow: 0 0 0 3px rgba(134, 239, 172, 0.1);
        transform: scale(1.02);
    }
</style>

<!-- Back Button -->
<div class="absolute top-4 left-4 z-50">
    <a href="@Url.Action("Login", "User")" class="flex items-center space-x-2 px-4 py-2 bg-white bg-opacity-90 hover:bg-opacity-100 text-green-700 rounded-lg shadow-md hover:shadow-lg transition-all duration-200 backdrop-blur-sm">
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"></path>
        </svg>
        <span class="font-medium">Back to Login</span>
    </a>
</div>

<div class="min-h-screen gradient-bg flex items-center justify-center p-4 relative overflow-hidden">
    <!-- Enhanced Decorative Background Elements -->
    <!-- Top section bubbles -->
    <div class="floating-circle-1 absolute top-20 left-10 w-24 h-24 bg-green-300 rounded-full opacity-30 blur-sm"></div>
    <div class="floating-circle-2 absolute top-16 right-20 w-16 h-16 bg-green-200 rounded-full opacity-20 blur-sm"></div>
    <div class="floating-circle-3 absolute top-32 left-32 w-32 h-32 bg-green-400 rounded-full opacity-25 blur-sm"></div>
    <div class="floating-circle-4 absolute top-8 right-40 w-12 h-12 bg-green-300 rounded-full opacity-40 blur-sm"></div>
    <div class="floating-circle-5 absolute top-24 left-64 w-20 h-20 bg-green-200 rounded-full opacity-35 blur-sm"></div>

    <!-- Middle section bubbles -->
    <div class="floating-circle-6 absolute top-1/2 left-8 w-28 h-28 bg-green-400 rounded-full opacity-20 blur-sm"></div>
    <div class="floating-circle-7 absolute top-1/2 right-12 w-18 h-18 bg-green-300 rounded-full opacity-30 blur-sm"></div>
    <div class="floating-circle-8 absolute top-1/3 left-20 w-14 h-14 bg-green-200 rounded-full opacity-25 blur-sm"></div>
    <div class="floating-circle-9 absolute top-2/3 right-32 w-22 h-22 bg-green-400 rounded-full opacity-35 blur-sm"></div>

    <!-- Bottom section bubbles -->
    <div class="floating-circle-10 absolute bottom-20 left-20 w-26 h-26 bg-green-300 rounded-full opacity-25 blur-sm"></div>
    <div class="floating-circle-1 absolute bottom-40 right-10 w-18 h-18 bg-green-400 rounded-full opacity-30 blur-sm"></div>
    <div class="floating-circle-2 absolute bottom-32 left-40 w-12 h-12 bg-green-200 rounded-full opacity-40 blur-sm"></div>
    <div class="floating-circle-3 absolute bottom-16 right-24 w-20 h-20 bg-green-300 rounded-full opacity-20 blur-sm"></div>
    <div class="floating-circle-4 absolute bottom-48 left-16 w-16 h-16 bg-green-400 rounded-full opacity-35 blur-sm"></div>

    <!-- Additional small accent bubbles -->
    <div class="floating-circle-5 absolute top-80 left-8 w-8 h-8 bg-green-300 rounded-full opacity-20 blur-sm"></div>
    <div class="floating-circle-6 absolute top-60 right-8 w-10 h-10 bg-green-400 rounded-full opacity-25 blur-sm"></div>
    <div class="floating-circle-7 absolute bottom-60 left-12 w-14 h-14 bg-green-200 rounded-full opacity-30 blur-sm"></div>
    <div class="floating-circle-8 absolute bottom-80 right-16 w-12 h-12 bg-green-300 rounded-full opacity-40 blur-sm"></div>
    <div class="floating-circle-9 absolute top-1/4 right-8 w-6 h-6 bg-green-400 rounded-full opacity-35 blur-sm"></div>
    <div class="floating-circle-10 absolute bottom-1/4 left-6 w-8 h-8 bg-green-200 rounded-full opacity-25 blur-sm"></div>

    <!-- Main Content Container -->
    <div class="w-full max-w-lg slide-in-animation">
        <div class="bg-white rounded-2xl shadow-2xl p-8 border border-green-100">
            <!-- Header Section -->
            <div class="text-center mb-8">
                <div class="w-20 h-20 bg-gradient-to-r from-green-500 to-green-600 rounded-full flex items-center justify-center mx-auto mb-4 pulse-animation">
                    <svg class="w-10 h-10 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 7a2 2 0 012 2m4 0a6 6 0 01-7.743 5.743L11 17H9v2H7v2H4a1 1 0 01-1-1v-2.586a1 1 0 01.293-.707l5.964-5.964A6 6 0 1121 9z"></path>
                    </svg>
                </div>
                <h1 class="text-3xl font-bold text-green-800 mb-2">Forgot Your Password?</h1>
                <p class="text-gray-600 text-lg">No worries! Enter your email address and we'll send you a reset link.</p>
            </div>

            <!-- Flash Messages -->
            @if (TempData["Flash"] != null)
            {
                <div class="mb-6 p-4 bg-amber-500 rounded-lg">
                    <p class="text-white text-center font-medium text-lg">@TempData["Flash"]</p>
                </div>
            }
            @if (!string.IsNullOrEmpty(ViewBag.Message as string))
            {
                <div class="mb-6 p-4 bg-red-50 border border-red-200 rounded-lg">
                    <p class="text-red-700 text-center font-medium">@ViewBag.Message</p>
                </div>
            }

            <!-- Forgot Password Form -->
            <form asp-action="ForgotPassword" method="post" class="mb-6">
                @Html.AntiForgeryToken()

                <div class="mb-6">
                    <label for="email" class="block text-lg font-semibold text-gray-700 mb-4">Email Address</label>
                    <div class="relative">
                        @* <div class="absolute inset-y-0 left-0 pl-4 flex items-center pointer-events-none">
                            <svg class="w-5 h-5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 12a4 4 0 10-8 0 4 4 0 008 0zm0 0v1.5a2.5 2.5 0 005 0V12a9 9 0 10-9 9m4.5-1.206a8.959 8.959 0 01-4.5 1.207"></path>
                            </svg>
                        </div> *@
                        <input type="email"
                               id="email"
                               name="email"
                               class="w-full pl-4 pr-4 py-4 border-2 border-gray-300 rounded-xl focus:outline-none transition-all duration-300 ease-in-out email-input text-lg"
                               placeholder="Enter your email address"
                               required />
                    </div>
                    <p class="text-sm text-gray-500 mt-3">We'll send a password reset link to this email</p>
                </div>

                <button type="submit" class="w-full py-4 bg-gradient-to-r from-green-600 to-green-700 text-white rounded-xl text-lg font-semibold hover:from-green-700 hover:to-green-800 transform hover:scale-[1.02] transition-all duration-200 shadow-lg hover:shadow-xl">
                    <span class="flex items-center justify-center">
                        <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 19l9 2-9-18-9 18 9-2zm0 0v-8"></path>
                        </svg>
                        Send Reset Link
                    </span>
                </button>
            </form>

            <!-- Back to Login Section -->
            <div class="text-center">
                <p class="text-gray-600 mb-4">Remembered your password?</p>
                <a href="@Url.Action("Login", "User")" class="text-green-700 font-semibold hover:text-green-800 hover:underline transition-colors duration-200 flex items-center justify-center mx-auto">
                    <svg class="w-4 h-4 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 16l-4-4m0 0l4-4m-4 4h14m-5 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h7a3 3 0 013 3v1"></path>
                    </svg>
                    Back to Login
                </a>
            </div>

            <!-- Additional Help -->
            <div class="mt-8 pt-6 border-t border-gray-200">
                <p class="text-center text-sm text-gray-500">
                    Need help?
                    <a href="#" class="text-green-700 font-medium hover:underline">Contact Support</a>
                </p>
            </div>
        </div>
    </div>
</div>

<script>
    // Auto-focus on email input
    document.addEventListener('DOMContentLoaded', function() {
        const emailInput = document.querySelector('input[name="email"]');
        if (emailInput) {
            emailInput.focus();
        }
    });
</script>