@{
    ViewBag.Title = "Verify OTP - MetroFlow";
    ViewData["HideLayoutParts"] = true; // hide navbar & footer
    var email = ViewBag.Email as string;
}
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<style>
    @@keyframes pulse-green {
        0%, 100% {
            box-shadow: 0 0 0 0 rgba(134, 239, 172, 0.7);
        }

        70% {
            box-shadow: 0 0 0 10px rgba(134, 239, 172, 0);
        }
    }

    @@keyframes float-gentle {
        0%, 100% {
            transform: translateY(0px);
        }

        50% {
            transform: translateY(-10px);
        }
    }

    @@keyframes slide-in {
        from {
            opacity: 0;
            transform: translateY(30px);
        }

        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    .pulse-animation {
        animation: pulse-green 2s infinite;
    }

    .float-animation {
        animation: float-gentle 3s ease-in-out infinite;
    }

    .slide-in-animation {
        animation: slide-in 0.6s ease-out forwards;
    }

    .otp-input:focus {
        border-color: rgb(22, 163, 74);
        box-shadow: 0 0 0 3px rgba(134, 239, 172, 0.1);
    }

    .gradient-bg {
        background: linear-gradient(135deg, #f0fdf4 0%, #dcfce7 50%, #bbf7d0 100%);
    }

    /* Custom OTP input styling */
    .otp-input {
        width: 60px;
        height: 60px;
        text-align: center;
        font-size: 24px;
        font-weight: bold;
        border: 2px solid #d1d5db;
        border-radius: 12px;
        transition: all 0.3s ease;
    }

        .otp-input:focus {
            outline: none;
            border-color: #16a34a;
            transform: scale(1.05);
        }

    @@media (max-width: 640px) {
        .otp-input {
            width: 45px;
            height: 45px;
            font-size: 18px;
        }
    }
</style>

<!-- Back Button -->
<div class="absolute top-4 left-4 z-50">
    <a href="@Url.Action("Login", "User")" class="flex items-center space-x-2 px-4 py-2 bg-white bg-opacity-90 hover:bg-opacity-100 text-green-700 rounded-lg shadow-md hover:shadow-lg transition-all duration-200 backdrop-blur-sm">
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"></path>
        </svg>
        <span class="font-medium">Back to Login</span>
    </a>
</div>

<div class="min-h-screen gradient-bg flex items-center justify-center p-4 relative overflow-hidden">
    <!-- Decorative Background Elements -->
    <div class="absolute top-20 left-10 w-20 h-20 bg-green-200 rounded-full opacity-20 float-animation"></div>
    <div class="absolute top-40 right-20 w-16 h-16 bg-green-300 rounded-full opacity-15 float-animation" style="animation-delay: 1s;"></div>
    <div class="absolute bottom-20 left-20 w-24 h-24 bg-green-100 rounded-full opacity-25 float-animation" style="animation-delay: 2s;"></div>
    <div class="absolute bottom-40 right-10 w-12 h-12 bg-green-400 rounded-full opacity-30 float-animation" style="animation-delay: 0.5s;"></div>

    <!-- Main Content Container -->
    <div class="w-full max-w-md slide-in-animation">
        <div class="bg-white rounded-2xl shadow-2xl p-8 border border-green-100">
            <!-- Header Section -->
            <div class="text-center mb-8">
                <div class="w-20 h-20 bg-gradient-to-r from-green-500 to-green-600 rounded-full flex items-center justify-center mx-auto mb-4 pulse-animation">
                    <svg class="w-10 h-10 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z"></path>
                    </svg>
                </div>
                <h1 class="text-3xl font-bold text-green-800 mb-2">Verify Your Identity</h1>
                <p class="text-gray-600 text-lg">We've sent a 6-digit code to</p>
                <p class="text-green-700 font-semibold text-lg">@email</p>
            </div>

            <!-- Flash Messages -->
            @if (TempData["Flash"] != null)
            {
                <div class="mb-6 p-4 bg-red-50 border border-red-200 rounded-lg">
                    <p class="text-red-700 text-center font-medium">@TempData["Flash"]</p>
                </div>
            }
            @if (ViewBag.Message != null)
            {
                <div class="mb-6 p-4 bg-blue-50 border border-blue-200 rounded-lg">
                    <p class="text-blue-700 text-center font-medium">@ViewBag.Message</p>
                </div>
            }

            <!-- OTP Form -->
            <form asp-action="VerifyOtp" method="post" class="mb-6">
                <input type="hidden" name="email" value="@email" />

                <div class="mb-6">
                    <label class="block text-lg font-semibold text-gray-700 mb-4 text-center">Enter Verification Code</label>
                    <div class="flex justify-center">
                        <input type="text" name="otp" maxlength="6" class="otp-input text-green-800" placeholder="000000" required autocomplete="off">
                    </div>
                    <p class="text-sm text-gray-500 text-center mt-3">Enter the 6-digit code sent to your email</p>
                </div>

                <button type="submit" class="w-full py-4 bg-gradient-to-r from-green-600 to-green-700 text-white rounded-xl text-lg font-semibold hover:from-green-700 hover:to-green-800 transform hover:scale-[1.02] transition-all duration-200 shadow-lg hover:shadow-xl">
                    <span class="flex items-center justify-center">
                        <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                        </svg>
                        Verify Code
                    </span>
                </button>
            </form>

            <!-- Resend Section -->
            <div class="text-center">
                <p class="text-gray-600 mb-4">Didn't receive the code?</p>
                <form asp-action="ResendOtp" method="post">
                    <input type="hidden" name="email" value="@email" />
                    <button type="submit" class="text-green-700 font-semibold hover:text-green-800 hover:underline transition-colors duration-200 flex items-center justify-center mx-auto">
                        <svg class="w-4 h-4 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15"></path>
                        </svg>
                        Resend Code
                    </button>
                </form>
            </div>

            <!-- Additional Help -->
            <div class="mt-8 pt-6 border-t border-gray-200">
                <p class="text-center text-sm text-gray-500">
                    Need help?
                    <a href="#" class="text-green-700 font-medium hover:underline">Contact Support</a>
                </p>
            </div>
        </div>
    </div>
</div>

<script>
    // Auto-focus on OTP input
    document.addEventListener('DOMContentLoaded', function() {
        const otpInput = document.querySelector('input[name="otp"]');
        if (otpInput) {
            otpInput.focus();
        }
    });

    // Handle OTP input - only allow numbers and limit to 6 digits
    document.querySelector('input[name="otp"]').addEventListener('input', function(e) {
        // Remove any non-numeric characters
        let value = e.target.value.replace(/[^0-9]/g, '');

        // Limit to 6 digits
        if (value.length > 6) {
            value = value.substring(0, 6);
        }

        // Set the cleaned value back to input
        e.target.value = value;
    });

    // Prevent non-numeric input on keypress
    document.querySelector('input[name="otp"]').addEventListener('keypress', function(e) {
        // Allow backspace, delete, tab, enter, and numeric keys
        if (!/[0-9]/.test(e.key) && !['Backspace', 'Delete', 'Tab', 'Enter'].includes(e.key)) {
            e.preventDefault();
        }
    });

    // Prevent paste of non-numeric content
    document.querySelector('input[name="otp"]').addEventListener('paste', function(e) {
        e.preventDefault();
        let paste = (e.clipboardData || window.clipboardData).getData('text');
        let numericPaste = paste.replace(/[^0-9]/g, '').substring(0, 6);
        e.target.value = numericPaste;
    });
</script>